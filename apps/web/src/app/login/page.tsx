import LoginForm from "@/components/flex/LoginForm";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import Image from "next/image";

export default function Login() {
  return (
    <main>
        <div>
            <LoginForm />
        </div>
    </main>
  );
}
